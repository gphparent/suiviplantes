name: Envoi vers TestFlight

# Declenchement manuel uniquement.
#
# Ce n'est pas un detail : le depot est public, donc n'importe qui peut y
# proposer une modification. Un workflow declenche par une poussee ou une
# demande de tirage exposerait la mecanique de signature a du code venu de
# l'exterieur. `workflow_dispatch` exige un droit d'ecriture sur le depot.
#
# GitHub ne transmet jamais les secrets aux workflows lances depuis une
# bifurcation, mais s'appuyer sur cette garantie plutot que sur le declencheur
# serait une precaution de moins pour rien.
on:
  workflow_dispatch:
    inputs:
      notes:
        description: "Note pour les testeurs (facultatif)"
        required: false
        type: string

jobs:
  televerser:
    name: Archiver et envoyer
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verifier que les secrets sont en place
        env:
          CLE: ${{ secrets.ASC_PRIVATE_KEY }}
          IDENTIFIANT: ${{ secrets.ASC_KEY_ID }}
          EMETTEUR: ${{ secrets.ASC_ISSUER_ID }}
          EQUIPE: ${{ secrets.ASC_TEAM_ID }}
        run: |
          manquants=""
          [ -n "$CLE" ]         || manquants="$manquants ASC_PRIVATE_KEY"
          [ -n "$IDENTIFIANT" ] || manquants="$manquants ASC_KEY_ID"
          [ -n "$EMETTEUR" ]    || manquants="$manquants ASC_ISSUER_ID"
          [ -n "$EQUIPE" ]      || manquants="$manquants ASC_TEAM_ID"
          if [ -n "$manquants" ]; then
            echo "Secrets manquants :$manquants"
            echo "A deposer dans Settings > Secrets and variables > Actions."
            echo
            echo "La cle d'API doit avoir le role Admin : l'export d'une"
            echo "archive de distribution l'exige, et App Manager echoue sur"
            echo "« Cloud signing permission error »."
            echo
            echo "ASC_TEAM_ID est le Team ID, dix caracteres majuscules."
            echo "Il ne vient pas de la meme page que la cle d'API :"
            echo "  developer.apple.com/account > Membership details > Team ID"
            echo
            echo "Il ne peut pas etre devine : la seule commande qui listerait"
            echo "les equipes, altool --list-providers, refuse l'authentification"
            echo "par cle d'API et n'accepte qu'un identifiant Apple."
            exit 1
          fi
          echo "Les quatre secrets sont presents."

      - name: Deposer la cle d'API
        env:
          CLE: ${{ secrets.ASC_PRIVATE_KEY }}
          IDENTIFIANT: ${{ secrets.ASC_KEY_ID }}
          EMETTEUR: ${{ secrets.ASC_ISSUER_ID }}
        run: |
          # `altool` cherche la cle a cet emplacement precis, sous ce nom precis.
          mkdir -p ~/private_keys
          FICHIER=~/private_keys/AuthKey_${IDENTIFIANT}.p8
          printf '%s\n' "$CLE" > "$FICHIER"
          chmod 600 "$FICHIER"

          # Verification de forme, sans jamais afficher la cle. Un refus 401 au
          # milieu de l'archivage ne dit pas laquelle des trois valeurs est en
          # cause ; ces controles-ci le disent en une seconde.
          souci=0

          if ! head -1 "$FICHIER" | grep -q 'BEGIN PRIVATE KEY'; then
            echo "ASC_PRIVATE_KEY ne commence pas par -----BEGIN PRIVATE KEY-----"
            echo "  Le contenu du .p8 doit etre colle en entier, ces lignes comprises."
            souci=1
          fi
          if ! tail -2 "$FICHIER" | grep -q 'END PRIVATE KEY'; then
            echo "ASC_PRIVATE_KEY ne finit pas par -----END PRIVATE KEY-----"
            souci=1
          fi
          if [ "$(wc -l < "$FICHIER")" -lt 3 ]; then
            echo "ASC_PRIVATE_KEY tient sur moins de trois lignes :"
            echo "  les retours de ligne du fichier ont ete perdus au collage."
            souci=1
          fi
          if openssl pkey -in "$FICHIER" -noout 2>/dev/null; then
            echo "Cle privee lisible et bien formee."
          else
            echo "La cle privee est illisible par openssl : contenu tronque ou altere."
            souci=1
          fi

          # Les identifiants ne sont jamais affiches, seulement mesures.
          longueur_id=${#IDENTIFIANT}
          longueur_em=${#EMETTEUR}
          echo "ASC_KEY_ID : $longueur_id caracteres (attendu 10)"
          echo "ASC_ISSUER_ID : $longueur_em caracteres (attendu 36, un UUID)"
          [ "$longueur_id" -eq 10 ] || { echo "  Longueur inattendue."; souci=1; }
          [ "$longueur_em" -eq 36 ] || { echo "  Longueur inattendue."; souci=1; }

          if [ "$souci" -ne 0 ]; then
            echo
            echo "Corrigez le ou les secrets signales avant de relancer :"
            echo "un refus 401 d'Apple vient presque toujours de la."
            exit 1
          fi

      - name: Eprouver les identifiants aupres d'Apple
        env:
          IDENTIFIANT: ${{ secrets.ASC_KEY_ID }}
          EMETTEUR: ${{ secrets.ASC_ISSUER_ID }}
        run: |
          # Xcode signale un refus 401 au milieu de l'archivage, sans dire
          # laquelle des trois valeurs est en cause. Un appel direct le dit en
          # une seconde, et confirme au passage que la fiche est visible.
          python3 Tools/tester-cle-asc.py \
            ~/private_keys/AuthKey_${IDENTIFIANT}.p8 \
            "$IDENTIFIANT" "$EMETTEUR" ca.serre

      - name: Archiver
        env:
          IDENTIFIANT: ${{ secrets.ASC_KEY_ID }}
          EMETTEUR: ${{ secrets.ASC_ISSUER_ID }}
          EQUIPE: ${{ secrets.ASC_TEAM_ID }}
        run: |
          set -o pipefail
          # `-allowProvisioningUpdates` avec une cle d'API laisse Xcode creer
          # lui-meme le certificat de distribution et le profil. C'est ce qui
          # remplace le passage par le Trousseau d'un Mac.
          xcodebuild archive \
            -project Serre.xcodeproj \
            -scheme Serre \
            -destination 'generic/platform=iOS' \
            -archivePath build/Serre.xcarchive \
            -allowProvisioningUpdates \
            -authenticationKeyPath ~/private_keys/AuthKey_${IDENTIFIANT}.p8 \
            -authenticationKeyID "$IDENTIFIANT" \
            -authenticationKeyIssuerID "$EMETTEUR" \
            DEVELOPMENT_TEAM="$EQUIPE" \
            CURRENT_PROJECT_VERSION=${{ github.run_number }} \
            | tee archive.log

      - name: Preparer les options d'export
        env:
          EQUIPE: ${{ secrets.ASC_TEAM_ID }}
        run: |
          cat > ExportOptions.plist <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key><string>app-store-connect</string>
            <key>teamID</key><string>${EQUIPE}</string>
            <key>uploadSymbols</key><true/>
            <key>signingStyle</key><string>automatic</string>
            <key>destination</key><string>export</string>
          </dict>
          </plist>
          PLIST

      # L'export est l'etape qui exige la signature de distribution, et donc
      # une cle d'API au role Admin. Un role Developer ou App Manager echoue
      # ici sur « Cloud signing permission error », alors meme que l'archivage
      # a reussi : Apple n'offre aucun reglage pour accorder l'acces aux
      # certificats de distribution infonuagiques a une cle non-Admin.
      - name: Exporter l'archive
        env:
          IDENTIFIANT: ${{ secrets.ASC_KEY_ID }}
          EMETTEUR: ${{ secrets.ASC_ISSUER_ID }}
        run: |
          set -o pipefail
          xcodebuild -exportArchive \
            -archivePath build/Serre.xcarchive \
            -exportPath build/export \
            -exportOptionsPlist ExportOptions.plist \
            -allowProvisioningUpdates \
            -authenticationKeyPath ~/private_keys/AuthKey_${IDENTIFIANT}.p8 \
            -authenticationKeyID "$IDENTIFIANT" \
            -authenticationKeyIssuerID "$EMETTEUR" \
            | tee export.log
          ls -la build/export

      - name: Envoyer vers App Store Connect
        env:
          IDENTIFIANT: ${{ secrets.ASC_KEY_ID }}
          EMETTEUR: ${{ secrets.ASC_ISSUER_ID }}
        run: |
          set -o pipefail
          IPA=$(find build/export -name '*.ipa' | head -1)
          test -n "$IPA" || { echo "Aucun .ipa produit"; exit 1; }
          echo "Envoi de $IPA"
          # Validation d'abord : elle attrape la plupart des refus avant
          # l'envoi, et coute quelques secondes.
          xcrun altool --validate-app -f "$IPA" -t ios \
            --apiKey "$IDENTIFIANT" --apiIssuer "$EMETTEUR"
          xcrun altool --upload-app -f "$IPA" -t ios \
            --apiKey "$IDENTIFIANT" --apiIssuer "$EMETTEUR"

      - name: Effacer la cle
        if: always()
        run: rm -rf ~/private_keys

      - name: Conserver les journaux
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: journaux-envoi
          path: |
            archive.log
            export.log
          retention-days: 14
